# Normalise line endings so a Windows checkout and a Linux CI runner agree.
* text=auto eol=lf

# Windows-only scripts must keep CRLF or they will not run.
*.bat text eol=crlf
*.cmd text eol=crlf

# Binary
*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
*.jar binary
